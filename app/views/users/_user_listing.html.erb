<% @users.each do |user| %>
<%= div_for(user) do %>
	<%=user.name%> <br>
	
	<%= form_for(user) do |f| %>
	<%= f.text_field :name%>
	<%= f.check_box :name%>
	<%= f.submit %>
	<% end %>
	<%= content_tag_for(:div, user) do %>

	<%=user.email%>
	<% end %>
<% end %>
